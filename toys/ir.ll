; ModuleID = 'main'
source_filename = "main"

define double @main() {
  %1 cond ...
  br cond, label %then, label %cond or merge or else
  
  then:
  br cond, label %then, label %cond or merge or else
  then
  cond:
  
  merge:
  cond:
}
